#include "main.h"


/**
 * main - Entry point of the program which prints the number of arguments
 * passed into it.
 *
 * @argc: Arguments count
 * @argv: Argument vector
 *
 * Return: 0 (Success)
 */
int main(int argc, char __attribute__((unused)) **argv)
{
	printf("%d\n", argc);

	return (0);
}
