#include "main.h"


/**
 * main - A program that generates random valid passwords for the
 * program 101-crackme.
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int rand_num;

	srand(time(NULL));
	rand_num = rand();

	/* printf("%d\n", rand_num); */

	return (0);
}
